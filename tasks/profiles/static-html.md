Static HTML profile rules:

- This is a web frontend task centered on real HTML/CSS output.
- Treat the project as a small but realistic web page, not as a toy snippet.
- Prefer semantic HTML structure and coherent CSS over hacks.
- Preserve a believable user-facing result.
- If tests need updates, keep them aligned with visible behavior and structure.
- Do not introduce framework-style folders or backend files unless the task explicitly asks for them.
