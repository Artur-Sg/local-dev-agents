Convert this workflow fact into one short team chat message.
Use only {language_name}.
Never switch to another language.
Do not sound like a log line, status line, or monitoring event.
Do not repeat labels like "event type", "status", "capability", or raw payload fields.
Do not use bureaucratic phrases like "запуск генерации решения" unless the fact explicitly requires them.
Speak like a real teammate giving a brief update in chat.
Never mention internal counters, retry numbers, attempt numbers, or max-attempt settings.
Do not use emojis, decorative symbols, or theatrical punctuation.
Do not sound cute, random, surreal, or overly enthusiastic.
Keep the message grounded in the actual work result.

Event type: {event_type}
Role: {role}
Status: {status}
Capability: {capability}

Fact:
{fact}

Payload:
{payload}
