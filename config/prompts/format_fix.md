Your previous response could not be applied.

Original task:
{original_task}

Current project files:
{current_project_files}

Allowed output paths:
{allowed_paths}

Parser error:
{error}

Your previous response:
{bad_answer}

Return the solution again using the required format.
- Follow these incremental delivery rules:
{incremental_rules}
- Write only to the allowed output paths listed above.
- Reuse and improve the current project files instead of replacing the project with a new layout.

{required_file_format}
