Fix failing tests by returning only the files that need to change.
Keep changes minimal and preserve the project structure.
For UI library tasks, preserve the current scaffold but prefer a meaningful improvement over a purely cosmetic patch if the failing state shows the library is still underpowered.
